"use client";
import Link from "next/link";
import { usePathname } from "next/navigation";
const links = [["/dashboard","Dashboard"],["/roadmap","Roadmap"],["/learn","Learning session"],["/practice","Practice"],["/assessments","Assessments"],["/progress","Progress"]];
export function AppShell({ children }: { children: React.ReactNode }) { const path = usePathname(); return <div className="min-h-screen"><header className="border-b border-slate-200 bg-white"><div className="mx-auto flex max-w-6xl items-center justify-between px-4 py-4"><Link href="/dashboard" className="text-xl font-bold tracking-tight text-indigo-700">pathwise</Link><nav aria-label="Main navigation" className="hidden gap-1 md:flex">{links.map(([href,label]) => <Link key={href} href={href} className={`rounded-lg px-3 py-2 text-sm font-medium ${path === href ? "bg-indigo-50 text-indigo-700" : "text-slate-600 hover:bg-slate-100"}`}>{label}</Link>)}</nav><Link href="/" className="text-sm font-medium text-slate-600">Sign out</Link></div></header><main className="mx-auto max-w-6xl px-4 py-8">{children}</main></div>; }
